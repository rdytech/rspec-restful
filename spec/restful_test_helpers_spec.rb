require 'spec_helper'

describe RestfulTestHelpers do
  it 'has a version number' do
    expect(RestfulTestHelpers::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
